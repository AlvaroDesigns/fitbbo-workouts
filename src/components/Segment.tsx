interface SegmentItem { value: string; label: string }
export function Segment({items,value,onValueChange}:{items:SegmentItem[];value:string;onValueChange:(value:string)=>void}){const activeIndex=Math.max(0,items.findIndex(item=>item.value===value));return <div className="segment" role="tablist"><span className="segment-indicator" style={{width:`${100/items.length}%`,transform:`translateX(${activeIndex*100}%)`}}/>{items.map((item,index)=><div className="segment-slot" key={item.value}>{index>0&&<span className={`segment-separator ${activeIndex===index||activeIndex===index-1?"hidden":""}`}/>}<button role="tab" aria-selected={item.value===value} className={`segment-item ${item.value===value?"active":""}`} onClick={()=>onValueChange(item.value)}>{item.label}</button></div>)}</div>}
