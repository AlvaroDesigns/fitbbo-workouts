import {Link} from "react-router-dom";import type{Exercise}from"../types/exercise";import{publicPath}from"../utils/paths";
export function ExerciseCard({exercise,image,sets,reps,restSeconds,prescription}:Readonly<{exercise:Exercise;image?:string;sets:number;reps:string;restSeconds:number;prescription?:string}>){return <Link to={`/exercise/${exercise.id}`} className="exercise-row"><img className="exercise-media" src={publicPath(image??exercise.image)} alt={exercise.name}/><div className="exercise-info"><h4>{exercise.name}</h4><p>{prescription??`${sets} series · ${reps} reps · ${restSeconds}s descanso`}</p></div><span className="row-arrow">›</span></Link>}
